.ui-view.md-padding
  h2 Просмотр и редактирование имеющихся багетов (картинки)

  md-grid-list(
  md-row-height="200px"
  md-cols-xs='1'
  md-cols-gt-xs="2"
  md-cols-gt-sm="3"
  md-cols-gt-md="4"
  md-cols-gt-lg="5"
  layout-xl="6"
  )
    md-grid-tile(
    md-rowspan="2"
    md-colspan="1"
    ng-repeat= "bag in vm.baguettes track by bag.id"
    )
      md-card
        img.md-card-image.image-size(ng-src='http://bit.ly/1Yu4VIb' alt='Baguette {{$index}}')
        md-card-content(align='center')
          div
            span(ng-if='!vm.editable') Цвет: {{bag.colour.name}}
            md-button(ng-if='vm.editable') Цвет
          p Материал: {{bag.material.name}}
          p Бренд: {{bag.brand.name}}
          p Ширина багета: {{bag.borderWidth}} мм.
        md-card-actions.pink-button(layout='row', layout-align='center center')
          md-button(ng-click='vm.editBaguette(bag)') Редактировать
          md-button(ng-click='vm.deleteBaguette(bag)') Удалить


