.baguette-view.scroll-y(
  resize='baguetteView'
  ng-style='{height: (baguetteView.windowHeight - baguetteView.offsetTop) + "px"}'
)

  h1 {{ vm.baguette.name }}

  .images
    .baguette-image(ng-repeat="image in vm.images track by image.id")
      img(ng-src="{{image.smallSrc}}")
