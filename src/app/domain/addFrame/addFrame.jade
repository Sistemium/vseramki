div.addFrame(layout='row' style='height:{{ windowHeight - 70 }}px')
  div.md-whiteframe-z1.custom-margin(layout='column' layout-align='top center' flex)

    div(layout='column' layout-align='top center' style='width: 100%')
      h2 Создание новой рамки

      searchable-select(
      label='Багет'
      model='vm.frame.baguetteId'
      model-name='Baguette'
      options='vm.baguettes'
      not-addable='true'
      on-close='vm.useClickedBaguette()'
      )


    div(ng-show='vm.frame.baguetteId' style='width: 100%')
      h2.center-text-input Свойства рамки
      form(name="frameAttrsForm" layout='column' layout-align='center center')

        md-input-container.input-margin
          label Название рамки
          input(type='text' ng-model='vm.frame.name' name='name')

        searchable-select(
        label='Размер'
        model='vm.frame.frameSizeId'
        model-name='FrameSize'
        options='vm.frameSizes'
        not-addable='false'
        )

        md-input-container.input-margin
          label Рыночная цена (₽)
            =' '
            span.required-field-asterisk *
          input(step='any' type='number' ng-model='vm.frame.highPrice' min='1' name='highPrice' input-clear='black' required)
          div(ng-messages="frameAttrsForm.highPrice.$error")
            div(ng-message="required") Поле не может быть пустым

        md-input-container.input-margin
          label Оптовая цена (₽)
          input(step='any' type='number' ng-model='vm.frame.lowPrice' min='1' name='lowPrice' input-clear='black' check-price="{{vm.frame.highPrice}}")
          div.custom-animation(ng-show="frameAttrsForm.lowPrice.$error.checkPrice")
            span.custom-warning Оптовая цена больше рыночной

        md-input-container.input-margin
          label Штук в коробке
            =' '
            span.required-field-asterisk *
          input(
          step='1'
          type='number'
          ng-model='vm.frame.packageRel'
          min='1'
          required
          name='packageRel'
          input-clear='black'
          )
          div(ng-messages="frameAttrsForm.packageRel.$error")
            div(ng-message="required") Поле не может быть пустым

        md-input-container.input-margin
          label Вес (кг.)
          input(
          step='0.1'
          type='number'
          ng-model='vm.frame.pieceWeight'
          min='1'
          name='pieceWeight'
          input-clear='black'
          )

        input(type='hidden' ng-model='paramsCheck' ng-required='!vm.paramsCheck')

    md-divider(flex style='width:100%' ng-if='vm.dupMessage.length')
      .alert-div.md-margin.md-whiteframe-1dp(flex)
        span {{vm.dupMessage}}

    md-button.md-raised(
    ng-click='vm.saveFrame()'
    ng-hide='frameAttrsForm.$invalid'
    ) Сохранить рамку
